{
  "_comment": "Placeholder content overlay. The real copy lives in WarmGun/content.local.json (git-ignored) and is bundled into the app at build time; without it every non-AI scene is a full-length landscape scene. A lane maps a library-relative path prefix to a clip type ('acts' or 'fullLength'), optionally forces an orientation, and for an acts lane names its checkbox.",
  "lanes": [
    { "prefix": "non_AI/alpha/special", "type": "acts", "orientation": "landscape", "label": "Special" },
    { "prefix": "non_AI/alpha/full", "type": "fullLength", "orientation": "landscape" },
    { "prefix": "non_AI/tall_cuts", "type": "fullLength", "orientation": "portrait" }
  ]
}
